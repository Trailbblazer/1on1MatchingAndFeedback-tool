{
  "name": "sauna-frontend-backend",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "install": "cd frontend && npm install && cd ../backend && npm install",
    "start": "cd frontend && npm run build && cp -R build ../backend/ && cd ../backend && node index.js",
    "heroku-postbuild": "npm install && npm start"
  }
}
